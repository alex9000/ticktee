<%= form_for [@ticket, @comment] do |f| %>
  <strong>New comment</strong>
  <%= f.error_messages %>
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
  
  <%= f.submit %>
<% end %>